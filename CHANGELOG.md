# 변경 이력

이 프로젝트의 모든 주요 변경을 기록한다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를,
버전 체계는 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## 버전 정책

`0.x`는 **사전 배포(pre-release)** 를 뜻한다. API·스키마·임계값이 예고 없이 바뀔 수 있다.

`1.0.0`은 다음 세 가지가 모두 충족될 때 부여한다. 셋 다 실제 운영 환경 없이는 불가능하다.

1. **실기기 검증 완료** — 마이크 캡처가 `FakeRecorder`가 아닌 실제 기기에서 확인된다. 절차는 [app/README.md](app/README.md) 참조.
2. **배포 환경 재캘리브레이션** — 화자 검증·딥페이크 탐지 임계값이 LibriSpeech가 아닌 실제 사용자 오디오 분포로 산출된다.
3. **다중 워커 실측** — 처리량·메모리가 단일 프로세스가 아닌 운영 구성(`--workers N`)에서 측정된다.

## [0.9.0] - 2026-09-11

Phase 1~8 기능 완성. 실험실(LibriSpeech) 조건에서 전 구간이 동작하고 측정됐으나,
운영 환경 검증은 끝나지 않았다.

### 추가

- **AI 추론 서버** (Phase 1~2) — VAD 전처리, 성문 임베딩 추출, pgvector 저장, 1:1 코사인 검증 API
- **점수 정규화·캘리브레이션** (Phase 6) — AS-Norm과 EER 기반 임계값 산출. 코어를 SpeechBrain ECAPA-TDNN에서 WeSpeaker ResNet34-LM(ONNX)으로 전환
- **다중 화자 분리** (Phase 7) — SepFormer 분리 후 등록 성문으로 타겟 화자를 선택. 선택 마진을 응답에 노출해 판정 신뢰도 신호로 쓴다
- **딥페이크 탐지** (Phase 8) — AASIST-L 1차 스크리닝과 스푸핑 감사 체계
- **Flutter 크로스플랫폼 앱** (Phase 3) — Android·iOS·Linux·macOS·Windows 5개 타깃 Thin Client
- **관리자 웹 대시보드** (Phase 4) — 지표 모니터링, 화자 DB, 오딧 트레일, 임계값 캘리브레이션
- **운영 문서** — 설치·기동·배포 전 점검 가이드([docs/09_Operations.md](docs/09_Operations.md)), 실기기 검증 절차 12항목

### 변경

- **서버 최적화** (Phase 5) — 추론 동시 실행에 상한을 두어 꼬리 지연을 예측 가능하게 만들었다
- **클라이언트 업로드 포맷** — FLAC을 런타임 협상 후 사용하고 미지원 시 WAV로 폴백한다. 무손실이라 성문 점수가 바뀌지 않는다
- **음성 향상(DeepFilterNet) 채택 보류** — 실측에서 오히려 악화돼 도입하지 않았다

### 수정

- **동시 요청의 절반이 실패하던 버그** (Phase 5) — 최우선 발견 사항이었다
- **silero-vad의 전역 스레드 변경으로 인한 성능 저하** (Phase 7)
- **딥페이크 탐지 도메인 불일치** — 학습 도메인 밖 오디오에서 오탐이 심해 기본 임계값을 0.7에서 0.999로 올렸다. 증상 완화이며 근본 해결은 도메인 적응이다
- **마이그레이션 재적용 실패** — 003이 멱등하지 않아 재실행 시 오류를 냈다
- **Linux 레벨 미터 오경고** — `record_linux`가 진폭을 주지 않아 정상 녹음 내내 "소리가 너무 작습니다"가 떴다

### 측정값 (LibriSpeech 기준)

| 항목 | 값 | 조건 |
| :--- | ---: | :--- |
| AS-Norm EER | 1.25% | dev-clean 40화자, K=200 |
| 혼합 음성 EER (분리 전 → 후) | 15.6% → 6.3% | 2인 혼합, 분리 + 타겟 선택 |
| 최대 처리량 (개선 전 → 후) | 11.6 → 18.5 req/s | 단일 프로세스, 24코어 |
| 업로드 전송량 절감 | 39% | FLAC 대 WAV |

이 값들은 조용한 환경의 영어 낭독 음성에서 나온 것이다. **출발점이지 최종값이 아니다.**

### 검증되지 않은 것

- 실기기 마이크 캡처. 권한 팝업, 기기별 샘플레이트, FLAC 인코더 지원 여부
- 다중 워커 구성의 처리량과 메모리
- 실제 서비스 조건(잡음, 짧은 발화, 다국어)에서의 임계값 적합성
- 2019년 이후 TTS 공격에 대한 딥페이크 탐지 일반화
- 3인 이상 혼합 음성. 분리 모델이 2화자 고정이다

### 결론 내지 못한 실험

- **분리 아티팩트 대응 AS-Norm 코호트** — 코호트를 분리 오디오로 맞춰도 EER이 바뀌지 않음을 확인했다(차이 0.06%p, 해상도 0.42%p 미만). 잔여 격차는 정규화로 흡수되지 않으며 백본 파인튜닝이 필요하다. GPU가 없어 미착수다

[0.9.0]: https://github.com/MacTechIN/voice_fingerprint_analyzer/releases/tag/v0.9.0
