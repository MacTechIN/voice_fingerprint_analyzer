# --- Web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.5
pydantic-settings==2.7.1

# --- Audio ---
soundfile==0.13.0
numpy<2.0

# --- ML (torch는 CPU 휠을 별도 인덱스에서 설치) ---
torch==2.5.1
torchaudio==2.5.1
speechbrain==1.0.2
requests==2.32.3          # speechbrain이 런타임에 쓰지만 의존성으로 선언하지 않음
# huggingface-hub 1.x는 use_auth_token 인자를 제거했으나 speechbrain 1.0.2가 아직 사용한다.
# 상한을 풀면 모델 적재가 TypeError로 실패하므로 speechbrain 업그레이드 전까지 고정한다.
huggingface-hub==0.26.5
silero-vad==5.1.2
# 음성 향상. 실측에서 화자 검증 EER을 악화시켜 기본 비활성이나(VG_ENHANCE_ENABLED),
# 재평가를 위해 의존성은 유지한다 (06 Phase 6 참조).
deepfilternet==0.5.6

# --- DB (Phase 2) ---
asyncpg==0.30.0

# --- Test ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
